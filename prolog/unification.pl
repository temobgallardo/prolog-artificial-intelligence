% vertical is a functor /1. 
% Here we say vertical is a line that has the same-X-coordinate
vertical( line( point(X, Y), point(X, Z) ) ).

% horizontal is a functor /1. 
% Here we say horizontal is a line that has the same-Y-coordinate
horizontal( line( point(X, Y) , point(Z, Y) ) ).

%  No permission to modify static procedure `(=)/2'
% =('mia',mia).
% 'mia' = mia. 